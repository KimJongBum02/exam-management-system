# 교수 프로그램 구조 리팩토링 — 적용 기록

작성일: 2026-09-21
작업 범위: `ExamManager/ProfessorUI` (학생 프로그램은 손대지 않음)
상태: **적용 완료.** `develop`(`1d3c1c7`, 팀원의 자동 연결·학생 UI 작업 포함) 위에 올렸고, 실행해서 동작을 확인했다.

[00-파일명-및-책임-정리안.md](00-파일명-및-책임-정리안.md)이 제안이라면, 이 문서는 실제로 적용한 결과와 그렇게 정한 이유다.

> **한 번 되돌린 적이 있다.** 9월 21일 오전에 같은 작업을 끝냈지만, 팀원 작업이 `develop`에 먼저 병합되어 충돌이 커서 보류하고 `git stash` 에 넣어 두었다(`stash@{0}`). 이후 최신 `develop` 위에서 아래 순서대로 다시 적용했다. 그때 얻은 교훈이 0번 항목이다.

---

## 0. 작업 순서

1. 죽은 코드 삭제 (`_Legacy` 폴더, 옛 UI만 참조하던 파일, DNS 방식 코드)
2. **기능을 ViewModel → Service 로 이동** (파일 → 시험 흐름 → 채팅·보고 순)
3. 자리 정리 — 폴더·네임스페이스 정돈, 한 파일에 여러 클래스 나누기
4. Model 정리 (도메인별 묶음, UI 타입 제거)
5. 화면(View) 이름 변경
6. ViewModel·Service 이름 변경, 폴더 분류

**이름 변경(5·6)은 반드시 마지막에 한다.** 파일이 통째로 움직여 거의 모든 줄이 충돌로 잡히기 때문에, 팀원 작업과 겹칠 가능성이 있으면 기능 이동(2)을 먼저 올리는 편이 안전하다.
단계마다 빌드했고, 2번부터는 실행해서 확인했다.

---

## 1. 폴더 구조

```
ProfessorUI/
├ App.xaml(.cs)      앱 시작. 패킷을 받아 서비스로 넘기는 일만 한다 (229줄 → 149줄)
├ Common/            RelayCommand, UiContext          (화면 상태가 없는 도우미)
├ Converter/         ProgramListNameConverter, FileIconConverter
├ Model/             AlertData, ChatData, ProgramData, QuizData
├ Service/           기능 19개
├ View/Professor/    .xaml + .xaml.cs 만
└ ViewModel/
   ├ Chat/     ChatViewModel, ChatTabViewModel, NoticeViewModel
   ├ Exam/     ExamReadyViewModel, SelectedFileViewModel, SendFileViewModel,
   │           ExamStartViewModel, ExamEndViewModel, ExamSummaryViewModel
   ├ Student/  StudentStatusViewModel, ScreenBoardViewModel, StudentScreenViewModel
   ├ Program/  WhiteListViewModel, BlackListViewModel
   ├ Quiz/     QuizViewModel
   └ Shell/    MenuEntryViewModel, ServerStatusViewModel, ViewModelBase
```

**규칙**
- `View`에는 화면 파일만 둔다. 뷰모델·모델·변환기는 각자 폴더로 옮긴다.
- 파일 하나에 클래스 하나. 짝이 되는 열거형(`AlertKind`, `ProgramSource`)만 같은 파일에 둔다.
- **Model 폴더만 도메인별 묶음 파일**을 쓴다(`QuizData.cs`에 `QuizRound`·`QuizResponse`·`QuizStudentTally`). 파일 맨 위에 무엇이 들어 있는지 한 줄 주석을 단다. 200줄이 넘거나 클래스가 4개 이상이면 다시 나눈다.
- ViewModel 폴더는 네임스페이스를 `ProfessorUI.ViewModel` 하나로 유지한 채 폴더만 나눈다. 그래야 `using` 을 건드리지 않는다.
- 화면이 바인딩하는 것은 전부 ViewModel, 데이터만 Model. (목록 한 줄짜리도 뷰모델이다 — 표시 문구와 화면 상태를 갖기 때문)

---

## 2. 화면(View) 이름

`ExamManager/ProfessorUI/View/Professor/`

| 이전 | 이후 | 무엇을 하는 화면인가 |
|---|---|---|
| ShellWindow | **MainWindow** | 주 창. 왼쪽 메뉴와 화면 전환 |
| DashboardPage | **DashboardWindow** | 메뉴 0번. 실시간 학생 현황 |
| ExamPrepPage | **ExamWizard** | 메뉴 1번. 시험 준비 3단계 마법사 |
| ChatPage | **AlertAndChat** | 메뉴 3번. 알림·채팅 |
| CheatAlertDetailPage | **CheatLog** | 경고 한 건 상세 |
| SecurityPolicyPage | **ProgramManageWindow** | 메뉴 4번. 허용·금지 목록 관리 |
| ExamEndPage | **ExamEndWindow** | 메뉴 5번. 시험 종료·답안 수집·학생 승인 |
| EndSummaryPage | **ExamResultWindow** | 종료 완료 현황, [새 시험 준비] |
| OXQuizPage | **QuizWindow** | 메뉴 6번. OX 퀴즈 출제·응답 현황 |
| ScreenMonitoringPage | **ScreenMonitoring** | 메뉴 7번. 학생 화면 타일 목록 |
| ScreenDetailWindow | **DetailMonitoring** | 학생 화면 크게 보기 |
| MonitorBoardWindow | **DisplayBoardWindow** | 강의실 큰 모니터용 현황판 |

이름을 그대로 둔 화면: `ExamManagePage`, `ProgramPickerWindow`, `ProgramListWindow`, `AlertListPanel`, `PortWindow`, `Styles.xaml`

**주의**
- 화면 이름을 바꿀 때 함께 고쳐야 하는 곳: `.xaml`의 `x:Class`, `.xaml.cs`의 클래스 이름과 생성자, 그 화면을 만드는 쪽(`new XxxWindow()`), `MainWindow.xaml.cs`의 메뉴 전환 `switch`.
- 바꾸려는 이름을 이미 다른 화면이 쓰고 있으면(예전에 `ExamEndWindow`가 그랬다) 두 파일을 한 번에 맞바꿔야 한다.
- `ScreenMonitoring`, `AlertAndChat`, `CheatLog`, `DashboardWindow`는 실제로는 창이 아니라 `MainWindow` 안에 끼워 넣는 `UserControl`이다. 끝말은 사용자가 정한 대로 두었다.

---

## 3. ViewModel 이름

| 이전 | 이후 | 맡는 일 |
|---|---|---|
| FileReadyViewModel | **ExamReadyViewModel** | 시험 파일 고르기·압축 (1단계) |
| SelectedExamItem | **SelectedFileViewModel** | 고른 파일·폴더 한 줄 |
| FileDistributeViewModel | **SendFileViewModel** | 학생별 배포 표 (2단계) |
| ExamOverviewViewModel | **ExamSummaryViewModel** | 여러 화면이 함께 쓰는 집계 |
| AnswerCollectViewModel + 승인 뷰모델 | **ExamEndViewModel** (하나로 합침) | 시험 종료+답안 수집 → 학생 승인 |
| StudentItemViewModel + StudentItem | **StudentStatusViewModel** (하나로 합침) | 학생 한 명의 상태 전부 |
| ScreenMonitoringViewModel | **ScreenBoardViewModel** | 화면 타일 목록 |
| StudentScreenItem | **StudentScreenViewModel** | 화면 타일 한 개 |
| ChatTabItem | **ChatTabViewModel** | 대화 한 건 |
| NoticeItem | **NoticeViewModel** | 전체 공지 한 건의 수신 현황 |
| MenuEntry | **MenuEntryViewModel** | 왼쪽 메뉴 한 줄 |
| OXQuizViewModel | **QuizViewModel** | 문제 입력 폼 |
| ServerViewModel | **ServerStatusViewModel** | 상단바 서버 상태 |
| RightPanelViewModel (클래스명 오타 `RightPanelViewMondel`) | **ViewModelBase** | 알림 기반 클래스 |

- 목록 한 줄짜리도 `…ViewModel` 로 끝낸다. `Item` 접미사는 뜻이 모호해져 쓰지 않는다.
- `ViewModelBase` 의 옛 이름 `RightPanel…` 은 지금 없는 옛 오른쪽 패널의 흔적이었다.

### 합친 두 가지

**① `StudentItem` → `StudentStatusViewModel`**
배포 표 한 줄과 현황판 학생이 같은 학생을 두 벌로 들고 있었다. 합치면서 동기화 코드 약 40줄(`CreateRow`, `OnStoreStudentsChanged`, 이름 변경 구독)이 사라졌다.
- 배포 표는 `StudentStore.Instance.Students`를 그대로 쓴다.
- 배포용 값을 학생 뷰모델에 추가: `IsDeployTarget`, `DeployStatus`, `DeployProgress`, `DeployProgressText`, `SendingSessionId`
- **체크 상자는 반드시 따로 둔다.** 배포 표는 `IsDeployTarget`, 승인 표는 기존 `IsSelected`. 하나로 합치면 배포 때 전체 선택한 것이 승인 대상까지 바꿔 놓는다.
- [새 시험 준비]에서 배포 상태도 초기화한다(그러지 않으면 다음 시험 준비 화면에 지난 시험의 "수신완료"가 남는다).

**② `AnswerCollectViewModel` → `ExamEndViewModel` 로 흡수**
팀원이 [시험 종료]와 [답안 일괄 수집]을 **한 버튼**으로 합쳐 두었고, 승인까지 같은 화면(`ExamEndWindow`)에서 이어진다. 두 뷰모델이 똑같이 갖고 있던 `IsContainerEnabled`가 하나로 줄었고, `UiContext` 속성도 `ExamEnd` 하나가 되었다.

### 합치면 안 되는 것
`ChatViewModel`과 `ChatTabViewModel`은 1 대 N 관계다. 목록 한 줄마다 자기 객체가 있어야 줄마다 다른 값(미읽음, 마지막 메시지)을 보여 줄 수 있다. 합치면 대화 목록의 정렬·필터가 불가능해진다. 같은 이유로 `NoticeViewModel`, `StudentScreenViewModel`도 그대로 둔다.

---

## 4. Service — 새로 만든 것과 이름

### 새 서비스 5개

| 서비스 | 맡는 일 | 옮겨 온 곳 |
|---|---|---|
| **ExamReadyService** | 회차 번호(`MMdd_T1`), 같은 내용 판정(지문), 압축 실행 지시, 준비 상태 기록, 배포 전 묶음 삭제 | ExamReadyViewModel |
| **SendFileService** | 학생별 전송, 중복 전송 판단, 진행률·실패 이벤트 전달, 시험 중 늦게 받은 학생 시험 시작 | SendFileViewModel |
| **ExamFlowService** | 시험 시작(감시 목록 → 시작 신호 → 압축 해제 순서), 시험 종료, 답안 수집 요청, 감시 목록 재전송, 학생 승인·PC 종료, 새 시험 준비 초기화 | ExamStartViewModel, ExamEndViewModel, ProgramManageWindow·ExamResultWindow 코드비하인드 |
| **ChatService** | 채팅 보내기·받기, 공지 번호 매기기, 공지 수신 회신 해석 | ChatViewModel |
| **StudentReportService** | 학생 접속·종료, 파일 수신·정리 보고, 감시 상태 보고, 부정행위 알림 해석(`FriendlyAlert` 포함) | App.xaml.cs |

기존 서비스 보강
- `ProgramPickerService.LoadForPickerAsync(...)` — 선택창 목록 만들기(STA 스레드, "추가됨"·"허용 불가" 표시, 정렬). ProgramPickerWindow 에서 이동
- `ProgramControlStore.AddPicked(...)` — 고른 항목 넣기, 반대 목록 충돌 돌려주기

### 이름 규칙

| 끝말 | 뜻 | 해당 |
|---|---|---|
| `…Service` | 일을 하는 것(통신·파일·절차) | Network, Server, AutoConnect, ExamReady, Zip, SendFile, ExamFlow, AnswerCollect, Chat, Quiz, ProgramPicker, StudentReport |
| `…Store` | 여러 화면이 함께 보는 목록 | Student, Alert, ProgramControl, ClassroomProgram |
| `…State` | 단계·준비 여부 같은 단순 상태 | Exam, SendFile |
| `…Native` | C++ DLL 연결만 하는 것 | Zip |

| 이전 | 이후 | 왜 |
|---|---|---|
| FileDeployService / FileDeployState | **SendFileService / SendFileState** | 뷰모델 `SendFileViewModel` 과 낱말을 맞춤 |
| DiscoveryResponder | **AutoConnectService** | 팀원 기능 이름("자동 접속")과 맞춰 눈에 띄게 |
| FileControlService | **ZipNative** | 실제로는 FileControl.dll 함수 선언 |
| ExamPackager | **ZipService** | 파일 모으기·암호 생성·.7z 만들기 |
| ExamPackageService | **ExamReadyService** | 화면(ExamReadyViewModel)과 짝 |
| ProgramCatalog | **ProgramPickerService** | 프로그램 선택창 전용 |
| QuizSessionService | **QuizService** | 퀴즈 화면과 짝 |
| ServerControl | **ServerService** | 서버 여닫기·주소·포트 |
| ClassroomPrograms | **ClassroomProgramStore** | 받은 설치 목록을 보관하는 쪽 |

압축 쪽은 세 층으로 읽힌다.
```
ExamReadyViewModel → ExamReadyService → ZipService → ZipNative → FileControl.dll
   (화면)            (번호·판정)        (절차)      (DLL 연결)
```

### 서비스가 지키는 것
- **창을 띄우지 않는다.** 실패는 예외 대신 결과값(`PackageResult`, `SendResult`, `SendProblem`)으로 돌려주고, 확인·결과 창은 뷰모델이 띄운다.
- 실패 이유는 문자열 비교가 아니라 **열거형**으로 돌려준다(화면이 표 문구를 고를 때 쓴다).
- 네이티브 스레드에서 올라온 이벤트는 그대로 흘려보내고, 화면 스레드로 넘기는 것(`Dispatcher.BeginInvoke`)은 뷰모델이 한다.

---

## 5. Model 정리

```
Model/
├ QuizData.cs      QuizRound, QuizResponse, QuizStudentTally
├ AlertData.cs     AlertItem, AlertKind
├ ChatData.cs      ChatMessageModel
└ ProgramData.cs   ProgramEntry, ProgramSource
```

- 서비스 파일에 섞여 있던 데이터 클래스를 떼어냈다(`AlertStore.cs`, `QuizService.cs`, `ProgramPickerService.cs`).
- **Model 에서 WPF 타입을 없앴다.** `ProgramEntry.Icon`(`ImageSource`)을 지우고, 아이콘은 `Converter/FileIconConverter.cs`가 실행 파일 경로로 그린다. 덤으로 선택창이 목록을 만들 때 아이콘을 전부 뽑지 않아 더 빨리 뜬다.
- `MenuEntry`는 화면 상태(잠김·툴팁)라 ViewModel 로 옮겼다. `StudentModel`은 쓰이지 않아 지웠다.
- **남은 예외:** Model 의 클래스들은 여전히 `INotifyPropertyChanged`와 표시 문구(`StateText`, `ResultText`, `SummaryText`, `Guide`)를 갖고 있다. 엄격히 하려면 화면별 래퍼 뷰모델이 필요해서, 이번에는 두기로 했다.

---

## 6. 함께 고친 버그

- **회차 번호가 빈 번호를 쓰지 않던 문제** — 아직 배포하지 않은 묶음이 있으면 그 번호를 그대로 재사용해서, 폴더에서 `T1`을 지워도 계속 `T2`가 만들어졌다. 지금 규칙: **폴더에서 비어 있는 가장 작은 번호**를 쓰고, 아직 배포하지 않은 내 묶음은 자기 번호를 비운 것으로 친다. 번호가 옮겨 가면 배포한 적 없는 옛 파일은 지운다. (`ExamReadyService.ChooseOutputPath`)

---

## 7. 팀원 작업과 맞춘 부분

최신 `develop` 에는 자동 연결(UDP 탐색)과 학생 UI 작업이 먼저 들어와 있었다. 아래는 그대로 살렸다.

- **시험 종료 + 답안 수집을 한 버튼으로 합친 변경** — 그 흐름을 유지하고 그 안에서만 서비스를 부르게 했다.
- **채팅의 `GetOrCreateTab`·`EnsureTabs`** (교수가 먼저 말 걸기) — 그대로 두고 통신만 서비스로 넘겼다.
- **`AutoConnectService`(옛 DiscoveryResponder), `PortWindow`, 서버 포트 안내 문구** — 이름만 바꾸고 동작은 건드리지 않았다.

---

## 8. 남은 일

- **학생 프로그램은 손대지 않았다.** 같은 규칙을 적용하려면 따로 진행해야 한다. 특히 `ProfessorDiscovery`(학생 쪽 자동 연결)는 교수 쪽 `AutoConnectService` 와 이름을 맞추기로 했으나 아직 그대로다.
- `ViewModelBase` 를 물려받는 뷰모델은 `ChatViewModel`·`QuizViewModel` 둘뿐이다. 나머지는 각자 `INotifyPropertyChanged` 를 구현하고 있어, 원하면 정리할 수 있다.
- Model 의 `INotifyPropertyChanged`·표시 문구(5번의 "남은 예외").
- 학생 쪽 죽은 코드(옛 시험 화면 조각 4개 등)는 그대로 남아 있다.
