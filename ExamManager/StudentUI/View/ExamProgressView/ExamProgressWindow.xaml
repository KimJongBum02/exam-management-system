<Window x:Class="StudentUI.View.ExamProgressView.ExamProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shared="clr-namespace:StudentUI.View.Shared"
        mc:Ignorable="d"
        Title="시험 진행 중" Width="280" Height="160"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="True" ResizeMode="NoResize" Topmost="True"
        Loaded="Window_Loaded">
    <Border Background="White" CornerRadius="10" Margin="10"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.2" ShadowDepth="2" BlurRadius="5"/>
        </Border.Effect>
        <Grid Margin="15,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: 학생 정보 + 로그아웃/종료 -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StudentInfo}" FontWeight="Bold" FontSize="14" Foreground="#333333" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding LogoutCommand}" Content="로그아웃" Background="Transparent" BorderThickness="0" Foreground="#D32F2F" FontSize="11" Cursor="Hand" VerticalAlignment="Center"/>
                    <Button Command="{Binding ExitCommand}" Content="종료" Background="Transparent" BorderThickness="0" Foreground="#888888" FontSize="11" Cursor="Hand" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Row 1: 시험 진행 중 + 현재 시각 -->
            <Grid Grid.Row="1" Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="#2E7D32" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="시험 진행 중" FontSize="12" FontWeight="SemiBold" Foreground="#2E7D32" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding CurrentTime}" FontSize="12" Foreground="#888888" VerticalAlignment="Center"/>
            </Grid>

            <!-- Row 2: 접속 상태 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,0">
                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,5,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#E53935"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="#43A047"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ConnectionStatusText}" FontSize="11" Foreground="#888888" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Row 3: 화면 전환 확인용 임시 버튼 (대기/준비) -->
            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <Button Command="{Binding GoToWaitingCommand}" Content="시험 대기 화면으로 전환"
                        Background="#E0E0E0" BorderThickness="0" Padding="5" Cursor="Hand" FontSize="11"/>
                <Button Command="{Binding GoToPrepCommand}" Content="시험 준비 화면으로 전환"
                        Background="#E0E0E0" BorderThickness="0" Padding="5" Cursor="Hand" FontSize="11" Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Row 4: 공용 알림/채팅 버튼 + 펼침 패널 (맨 하단) -->
            <shared:NotificationChatPanel Grid.Row="4"/>
        </Grid>
    </Border>
</Window>
