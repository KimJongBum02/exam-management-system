<UserControl x:Class="StudentUI.View.StudentExamView.StudentTimer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudentUI.View.StudentExamView"
             mc:Ignorable="d">

    <Border Background="#EFF6FF"
            BorderBrush="#BFDBFE"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16,14,20,14"
            Margin="0,0,0,12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.03"
                              BlurRadius="8" ShadowDepth="2"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                <Ellipse Width="8"
                         Height="8"
                         Fill="#2563EB"
                         VerticalAlignment="Center"
                         Margin="0,0,8,0"/>

                <TextBlock Text="{Binding ExamTime.StatusText, StringFormat={}{0} : }"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#1D4ED8"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Column="1"
                       x:Name="Timer"
                       Text="{Binding ExamTime.RemainingText}"
                       FontSize="22"
                       FontWeight="Bold"
                       FontFamily="Consolas, Arial"
                       Foreground="#1A56DB"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
