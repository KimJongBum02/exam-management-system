<UserControl x:Class="ProfessorUI.View.ExamView.ExaminationMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.View.ExamView"
             mc:Ignorable="d" 
             Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:ExamStartView DataContext="{Binding ExamStartVM}" Grid.Column="0" Margin="5,10,5,10"/>
        <local:AnswerCollectView DataContext="{Binding AnswerCollectVM}" Grid.Column="1" Margin="5,10,5,10"/>
        <local:ExamEndView DataContext="{Binding ExamEndVM}" Grid.Column="2" Margin="5,10,13,10"/>
    </Grid>
</UserControl>
