<UserControl x:Class="ProfessorUI.View.TopMainView.TopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.View.TopMainView"
             xmlns:dash="clr-namespace:ProfessorUI.View.DashBoardCardView"
             xmlns:timer="clr-namespace:ProfessorUI.View.CurrentTimeView"
             mc:Ignorable="d">
    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <dash:DashBoardCardView DataContext="{Binding DashBoardCardVM}"/>
            </Grid>

            <Grid Grid.Column="1">
                <timer:CurrentTimeView DataContext="{Binding CurrentTimeVM}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>