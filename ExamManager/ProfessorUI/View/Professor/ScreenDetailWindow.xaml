<Window x:Class="ProfessorUI.View.Professor.ScreenDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="화면 상세 보기"
        Width="920" Height="620"
        MinWidth="640" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Background="#1A1D21">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ProfessorUI;component/View/Professor/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── 헤더 ── -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock x:Name="NameText" FontSize="18" FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock x:Name="UpdatedText" FontSize="12"
                           Foreground="#888" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Content="✕  닫기" HorizontalAlignment="Right" VerticalAlignment="Center"
                    Click="Close_Click" Foreground="#CCC" Background="Transparent"
                    BorderThickness="0" FontSize="13" Cursor="Hand"/>
        </Grid>

        <!-- ── 화면 이미지 ── -->
        <Border Grid.Row="1" Background="#0D0F12" CornerRadius="8">
            <Grid>
                <!-- 이미지 -->
                <Image x:Name="ScreenImage" Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>

                <!-- 수신 전 안내 -->
                <TextBlock x:Name="WaitingText" Text="화면 수신 대기 중…"
                           Foreground="#555" FontSize="16"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ── 하단 바 ── -->
        <TextBlock Grid.Row="2" x:Name="ResolutionText"
                   Text="320 × 180 · JPEG" Foreground="#555" FontSize="11"
                   HorizontalAlignment="Right" Margin="0,8,0,0"/>
    </Grid>
</Window>
