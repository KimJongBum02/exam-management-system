<UserControl x:Class="ProfessorUI.View.CurrentTimeView.CurrentTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.View.CurrentTimeView"
             mc:Ignorable="d" >
    <Border Background="Transparent">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0">

            <TextBlock Text="{Binding CurrentDate}" 
                       Foreground="Black" FontSize="19" FontWeight="Medium" 
                       HorizontalAlignment="Center" Margin="0,0,0,4"/>

            <TextBlock Text="{Binding CurrentTime}" 
                       Foreground="Black" FontSize="35" FontWeight="Bold" 
                       HorizontalAlignment="Center" FontFamily="Consolas, Arial"/>

        </StackPanel>
    </Border>
</UserControl>