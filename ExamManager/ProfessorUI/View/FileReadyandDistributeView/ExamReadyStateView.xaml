<UserControl x:Class="ProfessorUI.View.FileReadyandDistributeView.ExamReadyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.View.FileReadyandDistributeView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderBrush="#5C6AC4" BorderThickness="1" CornerRadius="12" Background="White" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <!-- 타이틀 및 설명 (몇 번째 단계인지는 상단 미니 단계 표시가 알려 준다) -->
                    <TextBlock Text="시험 준비 상태로 전환" FontSize="24" FontWeight="Bold" Foreground="#1E1B4B" Margin="0,0,0,8"/>
                    <TextBlock Text="학생 PC를 대기 화면에서 시험 준비 화면으로 넘깁니다." FontSize="14" Foreground="#64748B" Margin="0,0,0,24"/>

                    <!-- 안내 박스 -->
                    <Border Background="#F8FAFC" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="주의 사항" FontSize="14" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                            <TextBlock Text="• 파일 배포보다 먼저 눌러야 학생이 파일 도착을 볼 수 있습니다." FontSize="12" Foreground="#64748B" Margin="0,0,0,4" TextWrapping="Wrap"/>
                            <TextBlock Text="• 접속 중인 학생 전체에게 전달됩니다." FontSize="12" Foreground="#94A3B8" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 하단 버튼 -->
                <Button Grid.Row="1" Command="{Binding ForceReadyStateCommand}" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="44"/>
                            <Setter Property="Background" Value="#5C6AC4"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="#5C6AC4"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="FontWeight" Value="Bold"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" 
                                            Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#4A58B2"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#3D499A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    준비상태로 전환
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
