<UserControl x:Class="ProfessorUI.View.FileReadyandDistributeView.FileDistributeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProfessorUI.View.FileReadyandDistributeView"
             mc:Ignorable="d">
    <Grid>
        <!-- 외곽 테두리: IsContainerEnabled=False 일 때 #E2E8F0(회색), #F8FAFC(연회색 배경) -->
        <Border IsEnabled="{Binding IsContainerEnabled}" BorderThickness="1" CornerRadius="12" Padding="20">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#F8FAFC"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#5C6AC4"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Grid Margin="0,0,0,16">
                        <!-- 숫자 2 아이콘 -->
                        <Border HorizontalAlignment="Left" Width="36" Height="36" CornerRadius="18">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#F1F5F9"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                            <Setter Property="Background" Value="#5C6AC4"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="2" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#94A3B8"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </Grid>

                    <!-- 제목 -->
                    <TextBlock Text="시험 파일 개별/일괄 배포" FontSize="24" FontWeight="Bold" Margin="0,0,0,8">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#94A3B8"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                        <Setter Property="Foreground" Value="#1E1B4B"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="준비된 암호화 파일을 선택한 학생의 PC로 전송합니다." FontSize="14" Foreground="#94A3B8" Margin="0,0,0,20"/>

                    <TextBlock Text="{Binding DeployStatusMessage}" FontSize="16" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,12"/>

                    <!-- 목록 헤더 및 상단 선택 버튼들 -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="학생 배포 목록" FontSize="14" FontWeight="Bold" Foreground="#94A3B8" VerticalAlignment="Center"/>

                        <Button Grid.Column="1" Command="{Binding SelectAllCommand}" Content="전체 선택" Background="#F1F5F9" Foreground="#94A3B8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="8,4" Margin="0,0,8,0" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                            <Setter Property="IsEnabled" Value="True"/>
                                            <Setter Property="Foreground" Value="#334155"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Grid.Column="2" Command="{Binding DeselectAllCommand}" Content="전체 해제" Background="#F1F5F9" Foreground="#94A3B8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="8,4" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                            <Setter Property="IsEnabled" Value="True"/>
                                            <Setter Property="Foreground" Value="#334155"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <!-- 학생 목록 Expander -->
                    <Expander Header="목록 펼치기/접기" IsExpanded="True" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,16">
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,8,0,0">
                            <ScrollViewer MaxHeight="160" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Students}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="80"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,8,0">
                                                    <CheckBox.LayoutTransform>
                                                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                                    </CheckBox.LayoutTransform>
                                                </CheckBox>

                                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Center" FontWeight="SemiBold"/>

                                                <ProgressBar Grid.Column="2" Value="{Binding ProgressValue}" Maximum="100" Height="8" Background="#E2E8F0" Foreground="#CBD5E1" BorderThickness="0" Margin="0,0,12,0" VerticalAlignment="Center">
                                                    <ProgressBar.Resources>
                                                        <Style TargetType="Border">
                                                            <Setter Property="CornerRadius" Value="4"/>
                                                        </Style>
                                                    </ProgressBar.Resources>
                                                </ProgressBar>

                                                <TextBlock Grid.Column="3" Text="{Binding StatusText}" FontSize="12" Foreground="#94A3B8" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </Expander>

                    <TextBlock Text="{Binding ValidationMessage}" Foreground="#EF4444" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16" Height="20"/>
                </StackPanel>

                <!-- 하단 핵심 배포 버튼 (DataTrigger로 Command 상태 제압 후 비활성화 고정) -->
                <Button Grid.Row="1" Command="{Binding StartDeployCommand}" Height="44" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#F1F5F9"/>
                            <Setter Property="Foreground" Value="#94A3B8"/>
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Content" Value="🔒 파일 준비 완료 후 활성화"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsContainerEnabled}" Value="True">
                                    <Setter Property="IsEnabled" Value="True"/>
                                    <Setter Property="Background" Value="#5C6AC4"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Content" Value="🚀 선택 학생 파일 배포 시작"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsContainerEnabled}" Value="True"/>
                                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#4A58B2"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsContainerEnabled}" Value="True"/>
                                        <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Background" Value="#3D499A"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>