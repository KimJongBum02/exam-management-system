<UserControl x:Class="ProfessorUI.View.NotificationView.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding DataContext.NotificationVM, RelativeSource={RelativeSource AncestorType=Window}}">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <TextBlock Text="🔔 부정행위 알림" FontSize="16" FontWeight="Bold" Foreground="#EF4444" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="10">
            <TextBlock Text="새로운 알림이 없습니다." Foreground="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20"/>
        </ScrollViewer>
    </Grid>
</UserControl>