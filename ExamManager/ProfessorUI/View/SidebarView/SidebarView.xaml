<UserControl x:Class="ProfessorUI.View.SidebarView.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="64" d:DesignWidth="1000">

    <Grid>
        <ListBox x:Name="MenuListBox" 
                 SelectedIndex="{Binding SelectedIndex}" 
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 VerticalAlignment="Center">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#64748B"/>
                    <Setter Property="Height" Value="48"/>
                    <Setter Property="Margin" Value="4,0"/>
                    <Setter Property="Padding" Value="16,0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                        CornerRadius="12" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#1A56DB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBoxItem>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="현황판" FontSize="16" VerticalAlignment="Center" FontWeight="Medium"/>
                    <Button Click="OpenMonitorWindow_Click" Cursor="Hand" Margin="12,0,0,0" ToolTip="보조 모니터용 현황판 띄우기">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="badge" Background="#E0E7FF" BorderBrush="#A5B4FC" BorderThickness="1" CornerRadius="8" Padding="6,3">
                                    <TextBlock x:Name="txt" Text="새 창 띄우기" FontSize="11" FontWeight="Bold" Foreground="#4338CA" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="badge" Property="Background" Value="#C7D2FE"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                        <Setter TargetName="badge" Property="Background" Value="White"/>
                                        <Setter TargetName="badge" Property="BorderBrush" Value="White"/>
                                        <Setter TargetName="txt" Property="Foreground" Value="#1A56DB"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </ListBoxItem>

            <ListBoxItem>
                <TextBlock Text="파일 배포" FontSize="16"/>
            </ListBoxItem>
            <ListBoxItem>
                <TextBlock Text="시험 시작/종료" FontSize="16"/>
            </ListBoxItem>
            <ListBoxItem>
                <TextBlock Text="프로세스 제어" FontSize="16"/>
            </ListBoxItem>
            <ListBoxItem>
                <TextBlock Text="출결 관리" FontSize="16"/>
            </ListBoxItem>
            <ListBoxItem>
                <TextBlock Text="OX 퀴즈" FontSize="16"/>
            </ListBoxItem>

        </ListBox>
    </Grid>
</UserControl>