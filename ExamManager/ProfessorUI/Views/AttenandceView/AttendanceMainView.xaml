<UserControl x:Class="ProfessorUI.Views.AttenandceView.AttendanceMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.Views.AttenandceView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <!-- 상단 컨트롤은 내용물 크기만큼만 차지 -->
            <RowDefinition Height="Auto"/>
            <!-- 간격 띄우기용 -->
            <RowDefinition Height="20"/>
            <!-- 하단 컨트롤은 남은 화면 공간을 꽉 채움 (그래야 내부 스크롤이 정상 작동함) -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 돌발 출석 컨트롤 -->
        <local:SuddenAttendanceView Grid.Row="0" />

        <!-- 2. 학생 현황 리스트 컨트롤 -->
        <local:StudentListView Grid.Row="2" />
    </Grid>
</UserControl>
