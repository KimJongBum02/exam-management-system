<UserControl x:Class="ProfessorUI.Views.AttenandceView.StudentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.Views.AttenandceView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- 상단 헤더 영역 (엑셀 출력 버튼 등) -->
                <RowDefinition Height="*"/>
                <!-- 리스트 영역 (남은 공간 모두 차지) -->
            </Grid.RowDefinitions>

            <!-- 1. 타이틀 및 엑셀 출력 버튼 -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="👥 전체 학생 출석 현황" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="엑셀 출력" Background="#10B981" Foreground="White" HorizontalAlignment="Right" Padding="10,5"/>
            </Grid>

            <!-- 2. 스크롤이 적용된 학생 리스트 -->
            <!-- ScrollViewer.VerticalScrollBarVisibility="Auto"로 설정하여 내용이 길어질 때만 스크롤 표시 -->
            <ListView Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="학번" Width="120" DisplayMemberBinding="{Binding StudentId}"/>
                        <GridViewColumn Header="이름" Width="100" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="좌석" Width="80" DisplayMemberBinding="{Binding SeatNumber}"/>
                        <GridViewColumn Header="상태" Width="100" DisplayMemberBinding="{Binding Status}"/>
                        <GridViewColumn Header="출석 응답" Width="120" DisplayMemberBinding="{Binding Response}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Border>
</UserControl>
