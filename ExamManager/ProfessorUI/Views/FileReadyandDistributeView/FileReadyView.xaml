<UserControl x:Class="ProfessorUI.Views.FileReadyandDistributeView.FileReadyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.Views.FileReadyandDistributeView"
             mc:Ignorable="d">
    <Grid>
        <Border BorderBrush="#5C6AC4" BorderThickness="2" CornerRadius="16" Background="White" Padding="24">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Grid Margin="0,0,0,24">
                        <Border HorizontalAlignment="Left" Width="44" Height="44" CornerRadius="22" Background="#5C6AC4">
                            <TextBlock Text="1" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#EEF2FF" CornerRadius="16" Padding="16,8">
                            <TextBlock Text="진행 중" Foreground="#4F46E5" FontSize="14" FontWeight="Bold"/>
                        </Border>
                    </Grid>

                    <TextBlock Text="파일 준비 및 암호화" FontSize="32" FontWeight="Bold" Foreground="#1E1B4B" Margin="0,0,0,16"/>
                    <TextBlock Text="배포할 원본 폴더를 선택하면, 자동으로 암호화 및 압축을 진행합니다." FontSize="18" Foreground="#64748B" Margin="0,0,0,40"/>

                    <TextBlock Text="원본 대상 폴더" FontSize="16" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,10"/>

                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding SelectedFolderPath, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Foreground="#334155" Padding="16,12" BorderBrush="#CBD5E1" BorderThickness="1" VerticalContentAlignment="Center">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Button Grid.Column="1" Command="{Binding SelectFolderCommand}" Width="60" Margin="12,0,0,0" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                            <Path Data="M4 4h6l2 2h8v12H4z" Fill="Transparent" Stroke="#475569" StrokeThickness="2" Width="24" Height="20" Stretch="Fill" Margin="0,2,0,0"/>
                        </Button>
                    </Grid>

                    <Border Background="#F8FAFC" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="12" Padding="24" Margin="0,0,0,32">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="{Binding CurrentStatusMessage}" FontSize="18" FontWeight="Bold" Foreground="#475569" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding ProgressText}" FontSize="18" FontWeight="Bold" Foreground="#5C6AC4" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar Value="{Binding ProgressValue}" Height="12" Background="#E2E8F0" Foreground="#5C6AC4" BorderThickness="0" Margin="0,0,0,16">
                                <ProgressBar.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </ProgressBar.Resources>
                            </ProgressBar>
                            <TextBlock Text="현재 파일: data_package.zip" FontSize="15" Foreground="#94A3B8"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Button Grid.Row="1" Command="{Binding StartProcessCommand}" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="54"/>
                            <Setter Property="Background" Value="#5C6AC4"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="#5C6AC4"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="FontSize" Value="20"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="0,16"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#4A58B2"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#3D499A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>

                    압축 및 암호화 시작
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>