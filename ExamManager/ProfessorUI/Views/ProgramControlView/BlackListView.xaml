<UserControl x:Class="ProfessorUI.Views.ProgramControlView.BlackListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.Views.ProgramControlView"
             mc:Ignorable="d" 
             Background="Transparent">

    <Border BorderBrush="#E2E8F0" BorderThickness="2" CornerRadius="16" Background="White" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="차단 프로그램 (블랙리스트)" FontSize="22" FontWeight="Bold" Foreground="#1E1B4B" Margin="0,0,0,8"/>
                <TextBlock Text="시험 중 학생 PC에서 실행 시 강제 종료되며 즉시 알림이 발송됩니다." FontSize="14" Foreground="#64748B" TextWrapping="Wrap"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <WrapPanel>
                    <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="16" Padding="16,8" Margin="0,0,10,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Chrome" Foreground="#B91C1C" FontSize="15" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="×" Foreground="#B91C1C" FontSize="18" FontWeight="Bold" Cursor="Hand" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="16" Padding="16,8" Margin="0,0,10,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Firefox" Foreground="#B91C1C" FontSize="15" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="×" Foreground="#B91C1C" FontSize="18" FontWeight="Bold" Cursor="Hand" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="16" Padding="16,8" Margin="0,0,10,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="KakaoTalk" Foreground="#B91C1C" FontSize="15" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="×" Foreground="#B91C1C" FontSize="18" FontWeight="Bold" Cursor="Hand" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" FontSize="16" Padding="12" BorderBrush="#CBD5E1" BorderThickness="1" VerticalContentAlignment="Center">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <Button Grid.Column="1" Content="+ 추가" Foreground="White" FontSize="16" FontWeight="Bold" Margin="12,0,0,0" Width="80" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#DC2626"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#B91C1C"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="BtnBorder" Property="Background" Value="#991B1B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
