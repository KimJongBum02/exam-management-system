<UserControl x:Class="ProfessorUI.Views.ExamView.ExaminationMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProfessorUI.Views.ExamView"
             mc:Ignorable="d" 
             Background="Transparent">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:ExamStartView Grid.Column="0" Margin="10"/>
        <local:AnswerCollectView Grid.Column="1" Margin="10"/>
        <local:ExamEndView Grid.Column="2" Margin="10"/>
    </Grid>
</UserControl>
